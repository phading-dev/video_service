export let HLS_SEGMENT_TIME = 6; // sec
export let MEDIA_CONTENT_LENGTH_LIMIT = 50 * 1024 * 1024 * 1024; // 50GB
export let ACCEPTED_MEDIA_TYPES = new Set(["mp4", "mov", "mkv", "aac"]);
export let SUBTITLE_ZIP_CONTENT_LENGTH_LIMIT = 50 * 1024 * 1024; // 10MB
export let ACCEPTED_SUBTITLE_ZIP_TYPES = new Set(["zip"]);
